export class FindAbsoluteValue{
    public static findAbsolute(x: number): number {
        if (x<0) {
            return -x;
        }
        if (x==0) {
            return x = 0;
        }
        return x;
    }
}